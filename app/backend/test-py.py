import urllib.request
import urllib.parse
import json

def set_trustline_flags():
    # Use python to call the horizon api or similar to debug
    # But wait, python might not have stellar-sdk installed. 
    pass

print("Python is working")
